name: Emperator CI

on:
  push:
    paths:
      - 'emperor/**'
  pull_request:
    paths:
      - 'emperor/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install project
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]
      - name: Run linters
        run: ruff check emperor/src
      - name: Type check
        run: mypy emperor/src
      - name: Run tests
        run: pytest emperor/tests
